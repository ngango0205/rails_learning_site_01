<%= form_for(current_user.follower_relations.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :followed_id, @user.id %></div>
  <%= f.submit t(".follow_submit"), class: "btn btn-primary" %>
<% end %>
