<%= form_for current_user.follower_relations.find_by(followed_id: @user.id),
             html: { method: :delete }, remote: true do |f| %>
  <%= f.submit t(".unfo_submit"), class: "btn" %>
<% end %>
