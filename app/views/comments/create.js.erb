$("#form-lesson").html("<%= escape_javascript(render('comments/comments', object: @lesson)) %>");
$("#all-comment").html("<%= escape_javascript(link_to (t('.all') + " #{@lesson.comments.count} " + t('.comments')), lesson_comments_path(@lesson.id)) %>");
